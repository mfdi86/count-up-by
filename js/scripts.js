// Backend Logic //
  var total = 0;
  var summands = [5, 10, 15, 20, 25, 30]
  for (var index = 0; index < summands.length; index += 5) {
  total += summands[index];
}
alert(total);
});

var total = 0;
var summands = [5, 10, 15, 20, 25, 30]
for (var index = 0; index < summands.length; index += 5) {
total += summands[index];
}
alert(total);
});
